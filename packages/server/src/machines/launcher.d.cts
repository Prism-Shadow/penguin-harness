/** Hand-written declarations for launcher.cjs (see its own doc for why it stays CommonJS). */
export function posixLauncher(nodeFlags?: string[]): string;
export function windowsLauncher(nodeFlags?: string[]): string;
