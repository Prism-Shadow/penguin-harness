# Docs and examples: roadmap items and a genuinely self-evolving demo

- The README roadmap (both languages) gains two items: Agent company and templates, and company-level self evolving.
- The self-improvement example under `examples/` was reworked to be genuinely self-evolving: the demo now ships two runnable scripts that let a local open-weight model score itself, edit its own files and re-run — the loop the landing page demonstrates — instead of a fixed illustrative transcript.
