# Unreleased

- [2026-08-11] Core: the default system prompt's Tool-use section gains a web-search tip — DuckDuckGo's HTML endpoint via curl as the first stop, Google News RSS search as the news fallback (`-sGL`, a redirect quirk found by live-testing both commands), and a Playwright-browser escape hatch for DuckDuckGo's occasional CAPTCHA page; shipped as same-day revisions through the kernel-version flow, so existing agents pick it up via Update kernel (#262, stacked on #260). ([details](2026-08-11-default-prompt-search-tip.md))

- [2026-08-11] Skills: new manual-install library skill `humanizer` — rewrites prose in any language into book/newspaper/encyclopedia register; a seven-principle drafting core backed by a 25-tell three-layer diagnostic catalog, six-language cues and a five-round measured case study as reference files; Office Productivity group, `preinstall: false` (#256). ([details](2026-08-11-humanizer-skill.md))

- [2026-08-11] Core + Web App: agent configs gain a dated **kernel version** — creation and restore stamp the defaults generation, a hash-pin guard test forces a dated bump on any defaults change, and an Update control beside "restore defaults" smart-merges newer defaults while keeping user-customized fields (reported by readable names; unknown generations conservatively kept), with outdated hints on the agent overview and list cards; the overview itself settles into ruled Kernel and Agent State sections — version values rendered prominent, snapshot import/export beside the State version, a click-to-copy state path, the active-session stat dropped (#260, #261, stacked on #257). ([details](2026-08-11-agent-kernel-version.md), [compat](2026-08-11-backward-compatibility.md))

- [2026-08-11] Core + Web App: Skills, Vault and Schedules adopt Memory's injection pattern — the default template carries only `{{VAULT}}` / `{{SKILLS}}` / `{{SCHEDULES}}` section placeholders, each expanding to a per-agent editable prompt behind an enable switch on its tab; the new Schedules prompt teaches the model to manage schedule TOMLs with the ordinary file tools (`{{SCHEDULE_LIST}}` roster included), and legacy baked templates keep working, with one-click migration where the old default wording is intact (#257). ([details](2026-08-11-prompt-injection-toggles.md), [compat](2026-08-11-backward-compatibility.md))

- [2026-08-11] Web App: UI polish — the initial-password banner gains a flat dismiss X persisted per user, the locked-model chip's hint shrinks to "type /model to switch models" with a pointer cursor, the agent list's Settings button adopts the canonical sidebar gear at label color, and the agent detail tabs settle into Overview / System Prompt / Runtime / Tools / Skills / Memory / Vault / Schedules with the list's stat icons mirroring that order at uniform, wider spacing, and the shared toggle switch drops its lingering post-click focus halo for a refined track/knob treatment (#252, #253, #254, #255, #259). ([details](2026-08-11-web-ui-polish.md))

- [2026-08-11] Web App: the chat details card gains a click-to-copy Session id, the schedule form adopts the Project-defaults model/workspace pickers and a searchable session dropdown, saving Project new-chat defaults toasts, and the missing `{{SHELL}}` placeholder is restored to the prompt-placeholder list (drift-proofed by a test against the real default prompt). ([details](2026-08-11-web-session-and-schedule-forms.md))

- [2026-08-11] Web App: leaving a still-running conversation (switch or "new conversation") no longer fires a phantom completion that reloaded the session/Agent lists — the reload trigger is now guarded by session identity, removing an occasional app-wide flicker right after the click (#242). ([details](2026-08-11-chat-phantom-idle-reload.md))

- [2026-08-10] Core: MCP Server support lands on the reserved `tools.mcpServers` seam — official TypeScript SDK v2, three transports (stdio / Streamable HTTP / legacy SSE), tools exposed as `mcp__<server>__<tool>` under the unchanged execution and approval contract, `readOnlyHint` → read-only permission, vault kept out of server processes, unreachable servers skipped with a warning; the agent settings page gains form-based MCP Server management with a connectivity test; session creation no longer blocks on MCP connects — the wait streams as `mcp_connect_begin/end` events (unified chat step row shared with compaction, CLI lines, a Trace-timeline span) and the toolset moves from `session_meta.tools` into a new `tool_list_ready` event (**breaking**: pre-split Traces' embedded tool record is no longer displayed) (#242; closes #239, #229). ([details](2026-08-10-mcp-servers.md))

- [2026-08-10] Web App: the chat header statistics move to the far right and double as the details-card trigger (numbers when wide, info icon when narrow, a live running-services count beside the elapsed time), the card gains a copyable Session id and lists the session's background processes with a working Stop (new `/processes` endpoints; deletion now ends a conversation's processes, idle eviction spares runtimes with live ones) and names the trace file path, typed-but-unsent new-chat text parks as sendable draft conversations in a sidebar Drafts group, docked panel swaps retract before expanding, the seeded admin password is stored in the data root and re-printed framed on every start until changed (CLI attach included), conversations open pinned to the bottom under the notice banner, the scheduled-task form's model/workspace/session pickers match the defaults dialog (searchable session dropdown), saving Project defaults toasts "已保存", copy feedback is unified onto the button, the `{{SHELL}}` placeholder returns to the prompt tab, benchmark case materials open expanded, and the locked model chip points at `/model`. ([details](2026-08-10-web-app.md))

- [2026-08-10] Web App: chat UX batch — the seeded admin password is persisted and re-printed at every server start until changed, header stats merge into a details card with a live background-process list (kill controls; processes die with the conversation and block idle eviction) and a trace-file deep link, typed new-chat text parks as sidebar draft conversations, docked panels retract before swapping, the stream stays bottom-pinned across resizes, and mid-run sends survive the steering completion race with the per-turn thinking level carried on queued follow-ups (#241, #227, #246). ([details](2026-08-10-web-chat-ux.md))

- [2026-08-07] Core: default `max_turns` becomes -1 (unlimited, SDK fallback aligned), request caps and the compaction threshold derive from the model's context window (vLLM-class small windows work; #218), the LLM retry ladder slows to a 2s base with "everything except auth retries" classification, and Trace appends are serialized with best-effort tolerant reads for damaged files (#215); each record now lands via a single `write(2)` and resumption heals crash-torn tails, so a big record can no longer be split at the 512 KiB chunk boundary (#249). ([details](2026-08-07-core-runtime.md))

- [2026-08-07] Memory: an agent keeps long-term notes between Sessions under `agent_state/memory/` — a user scope (`memory/user/`, read by every Session) plus one directory per Workspace, each with its own `MEMORY.md` index; only the indexes enter the context (capped at 200 lines / 25,000 chars per scope), bodies read on demand. A temporary Workspace gets the user scope only. The template's `{{MEMORY}}` placeholder expands to the agent's own `memory.prompt` (+ `memory.workspace_prompt` in a persistent Workspace), both editable on the Memory tab; a pre-Memory template injects nothing until its one-click placeholder insert. Agent settings gain a Memory tab: memories grouped by scope with view / delete (index lines pruned) / edit-via-chat. ([details](2026-08-07-memory.md))

- [2026-08-06] Desktop app: penguin brand icons on every platform, task-completion system notifications (renderer-only, desktop sessions), explicit single-user mode (user/member management rejected with `desktop_single_user` and hidden), and a bundled `penguin` CLI on PATH (automatic on deb; menu-driven install elsewhere, no system Node needed). ([details](2026-08-06-desktop-app.md))

- [2026-08-06] Admin proxy options: a "Proxy options" dialog (explicit Save) with independent "application" and "agent environment" switches sharing one proxy address (empty = follow the system proxy; no environment variable needed), loopback always exempt, live toggle, OS-proxy injection on desktop. ([details](2026-08-06-system-proxy-switch.md))

- [2026-08-06] Web App: directory browsing no longer compounds path segments under repeated clicks (listings bound to their own directory, sequenced picker requests, localized `dir_not_found`), saving Project new-chat defaults resets the new-conversation draft (typed text survives, open drafts reseed live), the avatar update dot gains a tooltip and reaches the collapsed rail, the model catalog opens with only DeepSeek expanded and remembers the user's toggles per Project, the model dialog shows the protocol path in the base URL field and unified vendor-protocol hints, and the auto-created session workspace is consistently named 临时工作区 / "temporary workspace" (penguin-sdk skill v19). ([details](2026-08-06-web-app.md))

- [2026-08-06] Landing homepage leads with the desktop app: platform-aware download CTA in the hero, closing CTA repointed, CLI one-liner install moved below the fold to the quick start; `/download` page unchanged. ([details](2026-08-06-landing-desktop-first.md))

- [2026-08-06] Models: Thinking Machines Lab's Inkling joins on OpenRouter and Fireworks AI, Fireworks AI gains DeepSeek V4 Flash 0731, and the OpenRouter + SiliconFlow GLM-5.1 gateway listings are delisted (Z.AI direct stays; existing Project configs unaffected); OpenRouter prices refreshed from the models API on 2026-08-07 (Inkling cached input $0.17, four drifted rows corrected); agenthub-models skill v11. ([details](2026-08-06-model-catalog-inkling-dsv4-flash-0731.md))

- [2026-08-06] Release tooling: repo versions realigned with the shipped 0.2.1, and the release workflow now refuses a tag push whose version does not match `package.json` (the drift that made every dev build nag about updates); the bump is documented as a release-prep step. ([details](2026-08-06-release-version-guard.md))

- [2026-08-03] Web App: at medium desktop widths the chat toolbar's Agents/Workspace actions stay icon-only until the large-screen breakpoint, so they no longer overlap the running indicator and live statistics (#151). ([details](2026-08-03-chat-toolbar-layout.md))

