/**
 * Types for esm-cjs-banner.mjs. The module is plain JavaScript because the bundler configs
 * and scripts/deploy.mjs load it directly; this lets packages/desktop's typechecked test
 * import the same constant the build uses instead of re-stating it.
 */
export declare const ESM_CJS_BANNER: string;
