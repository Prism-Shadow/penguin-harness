# Changelog

[English](CHANGELOG.md)

每个版本一行简述。逐版本的细节见 [`changelog/<version>/`](changelog/)。

- **0.2.8** — 2026-08-27。这一版让绑定的聊天渠道不再只是一根文本管道。图片与文件在飞书、Telegram 上双向流动——进来时就是网页端输入框提交的那种输入，出去的是一次运行「既提到又产出」的文件，这一对判据既挑出了那个真正是重点的产物，也让可被引导的回复不至于变成读取原语。回复以排版而非字面 `**bold**` 抵达：新增按绑定保存的 `renderMarkdown`（默认开启），用各渠道自己的标记渲染——Telegram 用 HTML，飞书用卡片的 JSON 2.0 富文本组件，QQ 用 `msg_type: 2`；渠道画不出来的构造会被转换而不是漏成原文，分段不会切断实体或代码围栏，平台拒绝排版时自动退回纯文本重发，因此回复绝不会因排版而丢失。另有两个按绑定保存的开关决定一次运行如何抵达聊天：每个非空行各发一条，以及压住过程记录、只发这次运行的最后一条回复。QQ 作为第三个渠道加入，扫码即可绑定；由于该平台不允许机器人主动发言，连接器按被动回复额度进行配给。消息之外：扩展机制（一个 `activate(ctx)` 加两个带类型的事件，每次 App 创建都会重新投递）、`penguin auth` 终端登录并以首次登录链接取代固定初始密码、技能与预置模型更新及未预期错误的红点待办且都能解除、所有 Markdown 界面渲染 LaTeX、机器人账号不再被某个 Session 永久占有、绑定面板把「什么都没发生」的三种不同成因分开、TokenDance 分组新增 Qwen 3.8 Flash，以及 CI 改为并行分片。([细则](changelog/0.2.8/))
- **0.2.7** — 2026-08-27。针对 0.2.6 所发内容的修复版本。0.2.6 引入的消息绑定现在可以在桌面端使用——桌面端的产物此前只声明了 `require`，没有声明 `__filename` 与 `__dirname`，飞书 SDK 读取自身 `package.json` 时因此以 `__dirname is not defined` 让每次连接失败；Telegram 也能从轮询冲突中恢复，此前恢复判定挂在从不冲突的 `getMe` 上，于是每秒重试一次、永远走不到退避；被渠道重复投递的消息也只会执行一次，判重依据是渠道自己的消息 id，而不是文本。桌面端还会自行把随包的 `penguin` 命令放上 PATH，并在每次启动时修复它——绝不改动不是自己写的 `penguin`，也绝不从留不住的位置安装。Workspace 已被删除时，命令会指名那个目录，而不再报 `spawn bash ENOENT`；POSIX 的 shell 解析补上了 Windows 分支早就有的回退链，没有 bash 的机器不会再让每条命令在执行前就失败。此外：排队中的跟进消息无论经哪条路径入队都可撤回；落地页与 README 中已不成立的文案得到更正，技能清单改由测试钉住；随包 CLI 纳入 CI 校验；`web.db` 的两个升级方向都有测试钉住。([细则](changelog/0.2.7/))
- **0.2.6** — 2026-08-27。两个 Flash 模型（`glm-5.3-flash` 按售卖它的每条线路各列一行，以及 `qwen3.8-flash`），旁边是新的 TokenDance 网关分组——它的「授权密钥」会直接在你的账号上签发一把密钥，不必再去控制台复制；同时为 OpenRouter 与 TokenDance 加上应用归属标识。子 Agent 从「看着」变成「对话」：模型与面板自带的输入框都能在运行中改向，逐轮的 `abort` 取代了 `kill_subagent`，子会话的审批会上浮给用户。`penguin` CLI 重建为运行中服务端的 HTTP/SSE 客户端，配一套读取与调度整台安装的命令族、注入到每条 Agent 命令的控制环境变量，以及架在其上的 `penguin-orchestration` 技能。Session 可绑定自建的飞书应用或 Telegram 机器人，两侧都不需要公网 URL。上下文圆环可点开，把当前上下文拆成六个部分。更新红点从最外层菜单一路指到执行更新的那个控件。侧边栏分组与模型分组都可拖动排序，侧边栏各分组独立翻页。独立的 Trace 页移除，改由对话的 Trace 面板阅读，导入迁至系统设置。新建 Agent 可选装技能库或项目目录中的技能，也可直接从快照包初始化。内核更新的单位改为设置页签。以及所有 Harness 状态文件均改为原子替换。另将 Windows 签名主体更正为 `NaisNet Technology Co., Ltd.`，并改为同时保留上一个主体的名单；已安装的 0.2.4 Windows 桌面端仍需手动重装才能跟上。0.2.5 曾为同一批改动打过标签：它发布了 npm 包，随后在 Windows 桌面任务上失败，未产出任何 GitHub Release 或安装包——因此 0.2.5 只存在于 npm，本目录中没有它。([细则](changelog/0.2.6/))
- **0.2.4** — 2026-08-21。会读图的 DeepSeek 模型（`deepseek-v4-flash-vision-exp`，同时 DeepSeek 客户端换用 Responses 协议），补上直连的 `claude-opus-5` 预设，新建分组时可一次导入端点提供的全部模型；聊天页重构为右侧与底部两个标签式 Dock，所有侧边面板与任意数量的服务端终端都是其中的标签页，终端画面在刷新后原样恢复；后台执行带回由 harness 注入的完成报告，配 kill 工具与进程列表上的服务链接；Project 级的沙箱命令策略在所有审批模式下都拒绝破坏性 shell 命令；用量图表重做为带时间范围与精度控件的时间序列；Memory 支持按作用域整组导出导入，并在对话中列出本次改动的记忆、提示词也写明了何时该记；系统设置合并为一个对话框，解释收进标题旁的圆圈问号；草稿页的示例改为填入输入框，旁边多了用户自己保存的提示词文件夹；MCP Server 支持逐服务器的权限档位；桌面安装包改由 bundle 打包，并在图形界面启动时导入登录 shell 的环境变量。([细则](changelog/0.2.4/))
- **0.2.3** — 2026-08-19。可以分叉、收回与观察的对话：从已完成的回复 fork 出新 Session，把排队中的转向与追问消息召回输入框重新编辑再发送，侧边栏逐行的实时 Session 状态，一次压缩重构（工具结果先跑完再压缩、被中断的压缩连同半成品摘要一并丢弃、摘要边生成边流入一个默认折叠的行、客户端重启后恢复的 Session 也能压缩），随处可选的思考等级、会话中切换前先说明代价的确认，以及 AgentHub 0.4.4 带来的 `max` 新档位，自定义模型的协议与视觉能力探测并配上逐模型的 fast mode，会话行重新回到悬停位的归档/删除按钮与右键菜单，聊天区拖拽上传附件，以及运行中实例的原地热更新。([细则](changelog/0.2.3/))
- **0.2.2** — 2026-08-11。跨 Session 的长期 Memory（逐 Agent 的用户与 Workspace 两个作用域，只注入索引，配 Memory 标签页），经 stdio / Streamable HTTP / SSE 的 MCP Server 支持并可在应用内管理，Skills/Vault/Schedules 改用 Memory 式的可编辑提示词与启用开关，带日期的配置内核版本以及「恢复」旁边那个不丢改动的智能更新，加固后的 core 运行时（默认轮次无限、由上下文窗口推导的限额、原子的 Trace 追加），桌面端与代理的打磨，一批 Web 体验改进，以及一次干净的依赖审计。([细则](changelog/0.2.2/))
- **0.2.1** — 2026-08-04。桌面应用：在未改动的服务端与 Web App 之上的 Electron 外壳（打开即登录、共享数据根目录、按根目录的单实例锁、三平台安装包镜像到 OSS），带镜像感知静态链接的落地页下载页，独立安装脚本与 `penguin update` 的下载源选择，能在畸形摘要下靠重试存活、且成本终于可见的压缩，随机预置的管理员密码配登录节流，可从 Agent 设置管理的 Skill，逐 Project 的新会话默认值，架在 SQLite trace 索引之上因而可扩展的 Traces 页，以及服务端最后几处 IO 热点的关闭。([细则](changelog/0.2.1/))
- **0.2.0** — 2026-08-03。每个目标一个封装好的安装包，在线与离线安装通用——现已镜像到阿里云 OSS 并带自动回退——被截断的工具输出可从 Session 草稿区恢复，面向长对话的聊天导航（shell 风格的 ↑ 输入召回、层叠的粘性运行标题、带悬停预览的缩略图刻度轨），能在刷新后存活并携带文件附件的转向，由数据库提供、侧边栏分组分页的会话列表，以及刷新后默认 deepseek-v4-flash 的 qianwenai / OpenRouter 模型目录。([细则](changelog/0.2.0/))
- **0.1.5** — 2026-07-30。面向全部五个平台的自包含离线安装包（Windows 自带 MinGit），输入区支持任意类型的文件附件、图片可抵达转向与目标陈述，除被拒绝的凭证之外每一种 LLM 失败都能在运行内恢复，默认系统提示词缩短约十分之一，web-design 的纸质编辑风主题与 SDK skill 中的思考/图片指引，以及刷新过的 README / 落地页安装叙事。([细则](changelog/0.1.5/))
- **0.1.4** — 2026-07-27。把 0.1.3 的全部功能集带到 npm——那是 0.1.3 自己从未抵达的地方：它的发布任务挂在一个断言硬编码构建日期的版本端点测试上，而只有发布任务才会打这个戳。另有博客图片改由兄弟社区仓库托管，而不再放在本仓库。([细则](changelog/0.1.4/))
- **0.1.3** — 2026-07-27。Windows 支持（shell 选择、`install.ps1`、win-x64 包），目标模式让 Task 循环直到目标达成，带实时调用图的停靠子 Agent 面板，带可见倒计时的 LLM 错误重连，以及应用内版本显示配更新检查与管理员自更新。([细则](changelog/0.1.3/))
- **0.1.2** — 2026-07-26。内置文件工具配 git 风格的编辑 diff，运行中转向与排队的后续消息，交接式的模型切换配逐请求思考等级，免费的 OpenRouter 模型，以及独立源的 Workspace HTML 预览。([细则](changelog/0.1.2/))
- **0.1.1** — 2026-07-22。模型目录新增 Gemini 3.6 Flash 与 3.5 Flash-Lite，按 Workspace 分组的聊天侧边栏，经 `penguin update` 的原地升级，以及 vLLM / Ollama / LlamaFactory 与 bento-slides 几个 Skill。([细则](changelog/0.1.1/))
- **0.1.0** — 2026-07-21。首个功能版本：Web App、落地页与文档站、模型目录，以及自我改进类 Skill。([细则](changelog/0.1.0/))
- **0.0.1** — 2026-07-19。首个打 tag 的版本；changelog 历史从该 tag 之后开始。
